# Room XI Connect — Deployment

The current production deployment guide lives at `docs/DEPLOYMENT.md`.

This repository includes legacy Supabase-focused documents from earlier iterations; they are not used by the current Express + PostgreSQL + Drizzle stack.
