**TL;DR:** solid MVP (Express + Vite + Drizzle/Neon + Capacitor + strong privacy/CSRF/rate limits). To be truly production-ready: **enforce secrets (no fallbacks), add server-side validation, observability (logging/error tracking/metrics), CI/CD, container, prebuilt server, and mobile API base/CORS.** See the drop-in code below.

[Download the audit report](sandbox:/mnt/data/prod-readiness-audit.md)

---

## Plan (pseudocode)

1. **Harden config & secrets**

   * Create `server/config/env.ts` with Zod to validate **required envs**; fail-fast in prod.
   * Remove dev fallbacks for `SESSION_SECRET` and `JWT_SECRET`.
2. **Observability**

   * Add `server/logger.ts` (pino + request-id).
   * Add `server/observability/metrics.ts` (Prometheus `/metrics`).
3. **Security middleware**

   * Add CORS allowlist (`server/middleware/cors.ts`) for web + Capacitor.
   * Add centralized error handler (`server/middleware/errorHandler.ts`).
   * Add lightweight Zod validator middleware (`server/middleware/validate.ts`) for payloads.
4. **Server wire-up**

   * Import env/logger/cors/metrics into `server/index.js`.
   * Enforce secure cookies, HSTS, trust proxy (already set), keep CSP/CSRF/rate-limits.
5. **Mobile API base**

   * Create `src/lib/config.ts` and switch `src/lib/api.ts` to use `API_BASE` (works for web + Capacitor).
6. **Ops**

   * Add **Dockerfile** (multi-stage, builds client, runs server).
   * Add **GitHub Actions** workflow for lint/test/build/docker.
   * Add `public/.well-known/security.txt` and `public/robots.txt`.
7. **(Near-term)** convert runtime TS to prebuilt JS for server (keep `tsx` dev, compile for prod).

---

## What to fix / change / add (high-impact first)

* **Secrets:** require `SESSION_SECRET`, `JWT_SECRET`, `DATABASE_URL`, `EMAIL_PROVIDER` (+ provider keys), `VAPID_*`. No dev fallbacks in prod.
* **Validation:** add request schemas (e.g., auth/checkins/profile/ximi/admin), reject invalid bodies early.
* **Logging & tracing:** structured logs, request-id, error handler, Sentry (optional).
* **Metrics:** `/metrics`, keep `/health/live` and `/health/ready`.
* **CORS & Mobile:** explicit allowlist (include `capacitor://localhost` when needed) and configurable `API_BASE`.
* **Build:** prebuild server for prod (avoid `tsx` in prod), or temporarily allow it in Docker and move to prebuild.
* **Admin:** ensure `ADMIN_PASSWORD` is bcrypt hash; consider 2FA/IP allowlist; strict limiter already present.
* **Public hygiene:** add `security.txt`, `robots.txt` (and `sitemap.xml` if SEO matters).
* **CI/CD:** run lint, tests (Vitest), type-check, build, docker build/push.

---

## Drop-in code (new files + minimal patches)

```ts
// path: server/config/env.ts
// WHY: Validate and centralize required configuration; fail fast in production.
import { z } from 'zod';

const Base = z.object({
  NODE_ENV: z.enum(['development','test','production']).default('development'),
  PORT: z.coerce.number().default(5000),
  DATABASE_URL: z.string().url(),
  SESSION_SECRET: z.string().min(32, 'SESSION_SECRET must be at least 32 chars'),
  JWT_SECRET: z.string().min(32, 'JWT_SECRET must be at least 32 chars'),
  EMAIL_PROVIDER: z.enum(['sendgrid','gmail']).default('gmail'),
  GMAIL_USER: z.string().optional(),
  GMAIL_APP_PASSWORD: z.string().optional(),
  SENDGRID_API_KEY: z.string().optional(),
  VAPID_PUBLIC_KEY: z.string().optional(),
  VAPID_PRIVATE_KEY: z.string().optional(),
  VAPID_EMAIL: z.string().default('mailto:security@roomxiconnect.org'),
  ALLOWED_ORIGINS: z.string().default(''), // comma-separated origins
  AI_INTEGRATIONS_OPENAI_API_KEY: z.string().optional(),
  AI_INTEGRATIONS_OPENAI_BASE_URL: z.string().optional(),
});

const env = Base.parse(process.env);

// Hard fail in production if provider creds missing
if (env.NODE_ENV === 'production') {
  if (env.EMAIL_PROVIDER === 'gmail') {
    if (!env.GMAIL_USER || !env.GMAIL_APP_PASSWORD) {
      throw new Error('GMAIL_USER and GMAIL_APP_PASSWORD required in production');
    }
  }
  if (env.EMAIL_PROVIDER === 'sendgrid') {
    if (!env.SENDGRID_API_KEY) throw new Error('SENDGRID_API_KEY required in production');
  }
  if (!env.VAPID_PUBLIC_KEY || !env.VAPID_PRIVATE_KEY) {
    throw new Error('VAPID_PUBLIC_KEY and VAPID_PRIVATE_KEY required in production');
  }
}

export default env;
```

```ts
// path: server/logger.ts
// WHY: Structured logs + request correlation for support & forensics.
import pino from 'pino';
import pinoHttp from 'pino-http';
import { randomUUID } from 'crypto';

export const logger = pino({
  level: process.env.LOG_LEVEL || 'info',
  transport: process.env.NODE_ENV === 'development' ? { target: 'pino-pretty' } : undefined,
  redact: { paths: ['req.headers.authorization','req.headers.cookie','password','*.password','token','*.token'], remove: true },
});

export const httpLogger = pinoHttp({
  logger,
  genReqId: (req) => req.headers['x-request-id'] as string || randomUUID(),
  customProps: (req, res) => ({ userId: (req as any).session?.userId ?? null }),
});
```

```ts
// path: server/middleware/cors.ts
// WHY: Allow mobile (Capacitor) + web origins explicitly.
import cors from 'cors';
import env from '../config/env.js';

const allowlist = (env.ALLOWED_ORIGINS || '')
  .split(',')
  .map(s => s.trim())
  .filter(Boolean);

export const corsMiddleware = cors({
  origin: (origin, cb) => {
    if (!origin) return cb(null, true); // same-origin/fetch from server
    if (allowlist.includes(origin)) return cb(null, true);
    return cb(new Error('CORS: Not allowed'), false);
  },
  credentials: true,
  methods: ['GET','POST','PUT','PATCH','DELETE','OPTIONS'],
  allowedHeaders: ['Content-Type','X-CSRF-Token','X-Requested-With'],
  optionsSuccessStatus: 204,
});
```

```ts
// path: server/middleware/errorHandler.ts
// WHY: Centralized safe error serialization; no stack leaks in prod.
import type { Request, Response, NextFunction } from 'express';
import { logger } from '../logger.js';

export function errorHandler(err: any, req: Request, res: Response, _next: NextFunction) {
  const status = err.statusCode || err.status || 500;
  const code = err.code || 'ERR_UNEXPECTED';
  logger.error({ err, status, code }, 'Request failed');

  if (process.env.NODE_ENV !== 'production') {
    return res.status(status).json({ error: code, message: err.message, stack: err.stack });
  }
  return res.status(status).json({ error: code, message: 'Internal server error' });
}
```

```ts
// path: server/middleware/validate.ts
// WHY: Simple Zod-based request validation middleware.
import type { Request, Response, NextFunction } from 'express';
import { ZodTypeAny } from 'zod';

export function validateBody(schema: ZodTypeAny) {
  return (req: Request, res: Response, next: NextFunction) => {
    const parsed = schema.safeParse(req.body);
    if (!parsed.success) {
      return res.status(400).json({ error: 'VALIDATION_ERROR', issues: parsed.error.issues });
    }
    req.body = parsed.data;
    next();
  };
}

export function validateQuery(schema: ZodTypeAny) {
  return (req: Request, res: Response, next: NextFunction) => {
    const parsed = schema.safeParse(req.query);
    if (!parsed.success) {
      return res.status(400).json({ error: 'VALIDATION_ERROR', issues: parsed.error.issues });
    }
    (req as any).query = parsed.data;
    next();
  };
}
```

```ts
// path: server/observability/metrics.ts
// WHY: Expose Prometheus metrics at /metrics (scrape target).
import client from 'prom-client';
import express from 'express';

client.collectDefaultMetrics();

export const metricsRouter = express.Router();
metricsRouter.get('/', async (_req, res) => {
  res.set('Content-Type', client.register.contentType);
  res.end(await client.register.metrics());
});
```

```diff
// path: server/routes/skip-token.js
// WHY: Remove dev fallback secret in production.
-const JWT_SECRET = process.env.JWT_SECRET || 'room-xi-dev-secret-change-in-production';
+const JWT_SECRET = process.env.JWT_SECRET;
+if (!JWT_SECRET) {
+  throw new Error('JWT_SECRET must be set');
+}
```

```diff
// path: server/index.js  (minimal edits; keep your existing code – add these)
+import env from './config/env.js';
+import { httpLogger, logger } from './logger.js';
+import { corsMiddleware } from './middleware/cors.js';
+import { errorHandler } from './middleware/errorHandler.js';
+import { metricsRouter } from './observability/metrics.js';
+import helmet from 'helmet';

 // ... inside createServer():
-  app.use(express.json());
+  app.use(httpLogger);
+  app.use(helmet({ crossOriginOpenerPolicy: { policy: 'same-origin' } })); // keep your CSP in security.ts
+  app.use(corsMiddleware);
+  app.use(express.json({ limit: '200kb' }));
   app.use(cookieParser());
   app.use(session({
     store: new PgStore({ pool, createTableIfMissing: true }),
-    secret: process.env.SESSION_SECRET || 'room-xi-dev-secret-DEVELOPMENT-ONLY',
+    secret: env.SESSION_SECRET,
     resave: false,
     saveUninitialized: false,
     cookie: {
       secure: process.env.NODE_ENV === 'production',
       httpOnly: true,
       sameSite: 'strict',
       maxAge: 1000 * 60 * 60 * 24 * 7,
     }
   }));
+  app.use('/metrics', metricsRouter);

 // ... after all routes:
+  app.use(errorHandler);

-  const port = process.env.PORT || 5000;
+  const port = env.PORT;
-  app.listen(port, '0.0.0.0', () => {
-    console.log(`🚀 Server running on http://0.0.0.0:${port}`);
+  app.listen(port, '0.0.0.0', () => {
+    logger.info({ port }, '🚀 Server started');
     initializeScheduler();
   });
```

```ts
// path: src/lib/config.ts
// WHY: One place to resolve API base for web + Capacitor.
const VITE_API_BASE = import.meta.env.VITE_API_BASE as string | undefined;

export const API_BASE =
  VITE_API_BASE ||
  (typeof window !== 'undefined' && window.location && window.location.origin
    ? `${window.location.origin}/api`
    : '/api');
```

```diff
// path: src/lib/api.ts (top of file)
// WHY: Use centralized API_BASE. Replace the hard-coded '/api'.
-const API_BASE = '/api';
+import { API_BASE } from './config';
```

```dockerfile
# path: Dockerfile
# WHY: Reproducible container for prod; builds client, runs server.
# --- builder ---
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build  # builds Vite client into /dist

# --- runner ---
FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production
COPY --from=builder /app/package*.json ./
RUN npm ci --omit=dev
COPY --from=builder /app/dist ./dist
COPY server ./server
# Ensure timezone/UTF-8
ENV TZ=Etc/UTC
EXPOSE 5000
CMD ["node", "--enable-source-maps", "--loader", "tsx", "server/index.js"]
```

```yaml
# path: .github/workflows/ci.yml
# WHY: CI – lint, test, build, and docker build.
name: CI
on:
  push: { branches: [main] }
  pull_request: { branches: [main] }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: 'npm' }
      - run: npm ci
      - run: npm run lint
      - run: npm test -- --run
      - run: npm run build
      - name: Docker build
        run: docker build -t roomxi/connect:${{ github.sha }} .
```

```txt
# path: public/.well-known/security.txt
# WHY: Standard contact for security reports.
Contact: mailto:security@roomxiconnect.org
Expires: 2030-01-01T00:00:00.000Z
Preferred-Languages: en
Policy: https://roomxiconnect.org/security
Acknowledgments: https://roomxiconnect.org/hall-of-fame
```

```txt
# path: public/robots.txt
User-agent: *
Disallow:
```

```
```

---

## Notes on rollout

* **ENV**: set `ALLOWED_ORIGINS` (comma list), e.g. `https://app.roomxiconnect.org,capacitor://localhost`.
* **Email**: prefer SendGrid/SES in prod; set DKIM/SPF; keep Gmail only for dev.
* **Admin**: set `ADMIN_USERNAME`, `ADMIN_PASSWORD` (bcrypt hash).
* **Mobile**: supply `VITE_API_BASE=https://api.roomxiconnect.org` at build time for Capacitor.

**a.** Want me to wire `zod` validation for your highest-risk endpoints next (auth/checkins/profile) and add tests?
**b.** Should I convert the server to a prebuilt TypeScript target (tsup config + scripts) so the Docker image drops `tsx` in prod?
