Task 1 – Server runtime & imports

Change "start" to use tsx in production:

"start": "NODE_ENV=production tsx server/index.js"


Fix server/middleware/applySecurity.ts to import ./security.ts instead of ./security.js.

Sanity-check internal imports so nothing points at non-existent .js files.

Task 2 – Rate limiting

Tighten authLimiter from 50 → ~15 requests / 15 minutes.

Add a dedicated adminLimiter (e.g. 5 per 15 minutes) and attach it to /api/admin.

Task 3 – Data retention cleanup

Add env vars in .env.example:

RETENTION_CHECKINS_DAYS=1095
RETENTION_XIMI_DAYS=365


Extend server/services/scheduler.js with:

A helper to compute cut-off dates.

A runDataRetentionCleanup function that:

deletes checkins older than cutoff,

deletes ximiConversations older than cutoff.

A shouldRunDataRetention time check (~03:00 Edmonton) to call that daily.

Task 4 – Moderation + unified Ximi helper

New server/services/moderation.ts:

Wraps OpenAI moderation (omni-moderation-latest or equivalent).

Extend server/services/ximi.ts:

Export CRISIS_KEYWORDS.

Import moderateText.

Add generateUnifiedXimiResponse(...):

keyword crisis pre-check,

moderation API check,

single adaptive system prompt with tone = soft / balanced / action,

handles "CRISIS_DETECTED_ESCALATE_NOW" safely.

Update /api/ximi/chat in server/routes/ximi.ts to use the unified helper, while keeping your existing DB structure intact (stores Ximi response + crisis flags/keywords as before).

Task 5 – Age recalculation from DOB

Add a small calculateAgeFromDob helper in server/routes/auth.js.

After successful login, recompute age from dateOfBirth and:

update profiles.age if it changed,

set req.session.requiresGuardianVerification = age < 16.

Task 6 – DB indexes

Add Drizzle indexes in server/schema.ts for:

checkins(userId, checkinDate)

ximiConversations(userId, createdAt)

programEvents(programId, dayOfWeek, startTime)

Task 7 – UX & accessibility polish

Clear up Explore gate copy (“why we encourage a check-in”, “skip for today”).

Add toggles in Settings for reduced motion / low-power visual mode.

Add a “New chat” / “Clear current thread” control in XimiChat UI.